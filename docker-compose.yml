services:
  servicebus-producer:
    build:
      context: .
      dockerfile: Dockerfile
    image: servicebus-producer
    container_name: servicebus-producer
    env_file:
      - .env
    ports:
      - "8005:8005"
    restart: unless-stopped
    networks:
      - shared

networks:
  shared:
    external: true
